<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark window for system night mode so the launch frame matches the dark theme
         (Compose applies the in-app theme; this just covers the pre-Compose frame). -->
    <style name="Theme.Spotter" parent="android:Theme.Material.NoActionBar">
        <!-- Matches DarkBackground. -->
        <item name="android:windowBackground">#FF0B0D10</item>
    </style>
</resources>
